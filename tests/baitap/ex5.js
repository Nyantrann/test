
for ( let i=1;i<=100; i++){
    if ( i % 2==0);
    console.log("số chẵn là:"+i);
    if ( i %2 !=0);
    console.log("so le la:"+i);
}
// a. Lặp từ 1 tới 100
// for (let i = 1; i <= 100; i++) {
//     // Biến 'i' là giá trị của vòng lặp hiện tại

//     // b. Kiểm tra điều kiện chẵn/lẻ
    
//     // Toán tử modulo (%) dùng để lấy số dư.
//     // Nếu số dư của i khi chia cho 2 bằng 0, tức là số chẵn.
//     if (i % 2 === 0) {
//         // i. Nếu giá trị của vòng lặp chia hết cho 2, in "Số <i> là số chẵn”
//         console.log(`Số ${i} là số chẵn`);
//     } else {
//         // ii. Nếu giá trị của vòng lặp không chia hết cho 2, in “Số <i> là số lẻ”
//         console.log(`Số ${i} là số lẻ`);
//     }
// }